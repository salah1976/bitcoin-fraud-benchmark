# Source Code

This directory contains the source code used to construct the benchmark.

Main components include:

* Snapshot extraction
* Temporal filtering
* Label generation
* Feature engineering
* Temporal address-memory computation

The goal is to provide a fully reproducible pipeline from raw blockchain data to the final benchmark used in the experiments.

